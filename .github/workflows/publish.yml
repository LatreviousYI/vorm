name: Publish to PyPI

on:
  push:
    branches: [ master ]
  workflow_dispatch:

permissions:
  id-token: write
  contents: read

jobs:
  check-version:
    name: Check version change
    runs-on: ubuntu-latest
    outputs:
      version-changed: ${{ steps.version-check.outputs.version-changed }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v5          # v4 → v5 (Node.js 24)
        with:
          fetch-depth: 2

      - name: Check if version changed
        id: version-check
        run: |
          CURRENT_VERSION=$(grep '^version = ' pyproject.toml | cut -d'"' -f2)
          PREVIOUS_VERSION=$(git show HEAD~1:pyproject.toml | grep '^version = ' | cut -d'"' -f2)
          if [ "$CURRENT_VERSION" != "$PREVIOUS_VERSION" ]; then
            echo "Version changed: $PREVIOUS_VERSION → $CURRENT_VERSION"
            echo "version-changed=true" >> $GITHUB_OUTPUT
          else
            echo "Version unchanged: $CURRENT_VERSION"
            echo "version-changed=false" >> $GITHUB_OUTPUT
          fi

  build:
    name: Build package
    needs: check-version
    if: needs.check-version.outputs.version-changed == 'true'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v5          # v4 → v5 (Node.js 24)

      - name: Install uv
        uses: astral-sh/setup-uv@v7         # v5 → v7 (Node.js 24)

      - name: Set up Python
        uses: actions/setup-python@v6       # v5 → v6 (Node.js 24)
        with:
          python-version-file: pyproject.toml

      - name: Build package
        run: uv build

      - name: Upload dist artifacts
        uses: actions/upload-artifact@v6    # v4 → v6 (Node.js 24)
        with:
          name: dist
          path: dist/

  publish:
    name: Publish to PyPI
    needs: build
    runs-on: ubuntu-latest
    environment: release
    steps:
      - name: Download dist artifacts
        uses: actions/download-artifact@v7  # v4 → v7 (Node.js 24)
        with:
          name: dist
          path: dist/

      - name: Install uv
        uses: astral-sh/setup-uv@v7         # v5 → v7 (Node.js 24)

      - name: Publish to PyPI
        run: uv publish --trusted-publishing always
